import { describe, test, expect } from 'vitest';

describe('@r-ishino/sample-ui', () => {
  test('placeholder test', () => {
    // このテストは、パッケージにテストファイルが必要なため作成されたプレースホルダーです
    // 実際のコンポーネントテストは今後追加されます
    expect(true).toBe(true);
  });
});
